{# form.html.twig #}
<form action="{{ path('home') }}" method="post" {{ form_enctype(form) }}>
    <fieldset>
        {{ form_widget(form) }}
        <button type="submit">Go</button>
    </fieldset>
</form>
